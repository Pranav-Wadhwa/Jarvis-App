[project]
name = "jarvis-app"
version = "0.1.0"
description = "LiveKit Agents App"
dependencies = [
    "livekit-agents[openai,silero,turn-detector]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.1",
    "langchain-community>=0.2",
    "unstructured[pdf,md]>=0.14",
    "langchain>=0.2",
    "deepgram-sdk",
    "livekit-plugins-deepgram",
    "livekit-plugins-rime",
    "livekit-plugins-openai",
]

[tool.uv]
python = "3.11"
