[project]
name = "jarvis"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[silero,turn-detector]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.1",
    "langchain>=0.2",
    "langchain-community>=0.2",
    "unstructured[pdf,md]>=0.14",
]
