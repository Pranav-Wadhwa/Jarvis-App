[project]
name = "jarvis"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[deepgram,rime,silero,turn-detector]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.1",
    "requests>=2.31.0",
]
